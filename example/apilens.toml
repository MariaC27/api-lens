[apilens]
# Dotted import path to your FastAPI app object
app = "example.main:app"

# Where to store snapshot JSON files (relative to project root)
snapshots_dir = "openapi-snapshots"

# Viewer host/port for `apilens serve`
host = "127.0.0.1"
port = 8765
